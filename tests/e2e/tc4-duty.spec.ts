import { test, expect } from '@playwright/test';

test('TC-4.2 Web: Admin Assign to Offline', { tag: '@tc4-assign-offline' }, async () => {
    console.log('Playwright: Running TC-4.2');
    expect(true).toBe(true);
});